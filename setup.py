# setup.py

import setuptools

# setup() will read most metadata from pyproject.toml automatically.
# This file mainly exists for compatibility with legacy tools that still expect setup.py.
# In modern Python packaging workflows, this file could even be empty,
# but keeping it improves compatibility.

setuptools.setup()